# 问题 2：BMS 放电 MOS 状态与电源板放电 MOS 的语义混淆

## 结论

这里的 BMS 是电池包内部的电池管理系统。`battery1_can_discharge_mos_state`
和 `battery2_can_discharge_mos_state` 是电池包 BMS 上报的内部放电 MOS 状态，
不是电源板本身的放电 MOS 状态。

电源板还有独立的本地放电 MOS：

- 电池 1：`BAT1_DISCHARGE_MOS_Pin`，即 `PA0`；
- 电池 2：`BAT2_DISCHARGE_MOS_Pin`，即 `PA5`。

两类 MOS 通常处于同一供电路径的不同位置：BMS 内部 MOS 决定电池包是否允许输出，
电源板 MOS 决定该电池包是否接入本板母线。因此 BMS 关闭自己的 MOS 时，电源板
无法仅靠本地 MOS 强行让该电池正常输出；但本地 MOS 是否保持开启仍然会影响后续
重新接通、故障恢复和母线安全状态。

## 当前代码的问题

`Power_IsBatteryCanReady()`（`Core/Src/gpio.c`）同时要求：

1. BMS 状态帧未超时；
2. BMS MOS 状态帧未超时；
3. BMS 上报的放电 MOS 状态为开启。

因此“CAN 通信超时”和“CAN 通信正常但 BMS 因过流、过温或其他保护主动关闭
内部放电 MOS”都会得到同一个 `false` 结果。

随后 `Power_DischargeModeTask()` 把这个结果解释为
`BATTERY_ALARM_STATUS_CAN_COMM_LOST`。如果本地状态已经是
`POWER_BATTERY_STATE_DISCHARGE`，又因为物理电压仍在线而保持电源板本地放电 MOS
开启。这样会出现以下风险：

- BMS 已明确禁止放电，但电源板没有把该事件当作“禁止放电”处理；
- 告警报文错误地报告为“CAN 通信掉线”；
- BMS 后续恢复或重新闭合内部 MOS 时，本地 MOS 可能已经处于开启状态，导致未经过
  新的完整决策就重新接入母线。

## 建议的判定模型

应至少拆成三个独立状态：

- `canCommunicationAlive`：状态帧和 MOS 状态帧是否在超时周期内更新；
- `bmsDischargeAllowed`：最近一次 BMS 报文是否允许电池放电；
- `localDischargeEnabled`：电源板自身的放电 MOS 是否开启。

只有在“CAN 通信确实超时、没有收到明确的 BMS 禁止放电状态、且电池物理电压仍
在线”时，才适用本次方案的“保持动力供电”策略。若 CAN 仍然正常但 BMS 上报内部
放电 MOS 关闭，应视为 BMS 保护/禁止放电事件，不能标记为纯 CAN 掉线并无条件保持
本地放电路径。

## 已确认的修复策略

用户已确认采用以下策略：

1. **CAN 正常且 BMS 允许放电**：按现有预放电和放电状态机运行。
2. **CAN 超时、物理电压在线**：认定为纯通信掉线；仅当本地路径已经处于 `POWER_BATTERY_STATE_DISCHARGE` 时保持本地放电 MOS，不从 `OFF` 状态新启动路径。
3. **CAN 正常但 BMS 明确关闭内部放电 MOS**：认定为 BMS 保护/禁止放电，使用独立状态码 `0x03`，立即关闭对应电源板本地放电 MOS 和回充 MOS；不调用双电池总关断，另一块电池继续独立运行。
4. **BMS 恢复允许放电**：对应本地状态必须重新经过 `OFF -> PRE_DISCHARGE -> DISCHARGE` 流程，不得因旧状态或本地 MOS 电平直接接回母线。
5. **BMS 状态未知或无效**：不得按 CAN 掉线保电；关闭对应本地路径并上报独立状态，收到明确允许状态后再重新预放电。
6. **双路预放电互斥**：两块电池均需独立经过预放电，但同一时刻只能有一路处于 `PRE_DISCHARGE`。
7. **MOS 状态优先级**：只要最近一帧 MOS 状态仍在有效期内，其中的明确禁放或未知值必须优先处理，不能因另一类 BMS 状态帧超时而退化为 CAN 掉线保电。

## 实施前置条件

1. 确认 BMS MOS 状态帧中的 bit/byte 定义，确认 `0` 严格表示 BMS 禁止放电；若存在初始化、未知或无效值，必须增加有效标志，不能直接当作禁止放电。
2. 确认 BMS 内部 MOS 与电源板本地 MOS 的实际串联位置、默认电平和故障复位行为。
3. 实测 BMS 内部 MOS 断开后电源板 ADC 电压：如果会低于物理在线阈值 60V，必须在状态判定中优先区分“BMS 禁止放电”和“物理拔出”，避免将 BMS 保护误报为拔出。

## 告警报文状态码

`0x04400000` 扩展帧的 Byte 0/1 分别对应电池 1/2：

- `0x00`：正常；
- `0x01`：CAN 通信掉线，物理在线，本地动力路径按策略保持；
- `0x02`：电池物理拔出，对应本地路径已切断；
- `0x03`：BMS 明确禁止放电，对应本地放电 MOS 和回充 MOS 已关闭。
- `0x04`：BMS 放电 MOS 状态未知或无效，对应本地放电 MOS 和回充 MOS 已关闭。
